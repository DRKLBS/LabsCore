before_install:
  - wget https://github.com/sormuras/bach/raw/master/install-jdk.sh
  - source ./install-jdk.sh --feature 17
  - ./gradlew shadowJar
jdk:
  - openjdk17

dependencies:
  - implementation: com.github.DRKLBS:LabsCore:main-SNAPSHOT

build:
  publish: true